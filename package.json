{
  "name": "liminal-space",
  "version": "1.0.0",
  "description": "AAA급 심리 공포 게임 - 리미널 스페이스",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "build:wasm": "cd wasm && wasm-pack build --target web --out-dir ../src/wasm"
  },
  "dependencies": {
    "@webgpu/types": "^0.1.55"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.3",
    "vite": "^5.0.8"
  }
}
